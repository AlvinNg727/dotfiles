import:
  - ~/.config/alacritty/one_dark.yaml

env:
  TERM: alacritty

window:
  opacity: 0.5

font:
    family: "JetBrainsMono Nerd Font"

cursor:
  style:
    # shape: Block
    # blinking: Off
    shape: Underline

shell:
  program: /usr/bin/fish
